# Loaded automatically by `npm test` / `npm run test:debug` via `node --env-file=env.test`.
# Override locally or in CI when Redis is not on localhost:6379.

PORT="3000"
REDIS_URL="redis://127.0.0.1:6379"